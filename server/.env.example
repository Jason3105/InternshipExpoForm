# Server Configuration
PORT=5000

# Google Sheets & Drive IDs
SPREADSHEET_ID=your_google_sheet_id_here
DRIVE_FOLDER_ID=your_google_drive_folder_id_here

# OAuth Credentials (from Google Cloud Console - OAuth 2.0 Client)
GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:5000/oauth2callback
JAVASCRIPT_ORIGIN=http://localhost:3000

# Service Account Credentials (Base64 encoded JSON)
# To generate: 
# On Linux/Mac: base64 -w 0 your-service-account-file.json
# On Windows PowerShell: [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Content your-service-account-file.json -Raw)))
SERVICE_ACCOUNT_KEY=your_base64_encoded_service_account_json_here

# Instructions:
# 1. Copy this file to .env
# 2. Fill in all the values with your actual credentials
# 3. Never commit .env to git (it's in .gitignore)
